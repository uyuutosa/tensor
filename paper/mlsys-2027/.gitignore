*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
# main.pdf is committed (viewable inline on GitHub blob viewer); see PR
# for rationale. Rebuild via `make` after every .tex/.bib change and
# commit alongside the source.
